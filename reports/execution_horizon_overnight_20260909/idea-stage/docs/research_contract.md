# 执行中新反馈的提前重规划

用户授权北京时间2026-09-09 03:12:30至09:12:30，六小时自主迭代Predictor A。沿用已选定的研究方向，不重新执行已完成的单次介入、接力诊断或旧架构实验。

研究问题：同一已生成动作chunk执行一部分后，新的本体感知是否提供调用时缓存特征之外的信息，使继续/提前重规划的完整闭环成功与实际成本获得更好的权衡？当前没有新结果，不预设新模块有效，也不将更频繁重规划本身称为新反馈收益。

首版A/VLA冻结，H>5时在执行第5步读取一次新proprio/夹爪状态，由一个小模块决定继续原名义H的剩余动作或提前调用A重规划。每chunk最多判断一次，不延长原H，不新增视觉编码器，不使用仿真器特权物体状态作为输入。缓存的A任务/视觉上下文、动作序列和旧proprio可保留。

训练监督来自同一真实中途状态的continue/replan成对完整续段与真实任务成功、RPC时间；源轨迹A、动作生成路径相同，后续由A接力。部署时不得使用分支回报、未来轨迹或特权状态。分支结果只用于训练与局部机制分析，完整同批开发闭环才用于评价模型。

基线为原A及同容量遮蔽新反馈的监视器；只在early训练范围确定配置、校准一次后锁模。训练300–329、early330–335、开发336–345、预留346–365边界保留，不自动消耗预留集。

成功与失败都归档，负结果可触发有证据的方向调整，但不靠扫描次数、阈值、checkpoint或扩大检查矩阵拖延。模型研究复盘使用同一独立Codex reviewer的连续记录；同家族判断标为provisional，不声称跨家族接受。到截止时间停止新增实验、保留未完成输出并给出事实总结。新训练如进行仅限新增小模块，原A/VLA保持冻结。
